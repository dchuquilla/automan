<div class="row">
  <div class="columns">
    <h1 class="text-headline">¿Olvidó su clave?</h1>
    <p>Ingrese la dirección de correo electrónico que usó para registrar su cuenta de usuario, en su buzón de correo recibirá las instrucciones para realizar el cambio de clave.</p>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= devise_error_messages! %>

      <div class="field">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <div class="actions">
        <%= f.submit "Enviar", class: "button" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
