<%= form_with(model: user_car_setting, url: car_maintenance_history_user_car_setting_path(@car_selected, @maintenance_history, user_car_setting), local: true) do |form| %>
  <% if user_car_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_car_setting.errors.count, "error") %> prohibited this user_car_setting from being saved:</h2>

      <ul>
      <% user_car_setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :km_estimated %>
    <%= form.number_field :km_estimated %>
  </div>

  <div class="field">
    <%= form.label :month_estimated %>
    <%= form.number_field :month_estimated %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar", class: "button" %>
  </div>
<% end %>
